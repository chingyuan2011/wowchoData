{
  "$jsonSchema": {
    "required": ["title", "createTime", "updateTime"],
    "properties": {
      "title": {
        "bsonType": "string",
        "description": "title is required, should be string"
      },
      "content": {
        "bsonType": "string",
        "description": "should be string"
      },
      "createTime": {
        "bsonType": "number",
        "description": "createTime is required, should be number"
      },
      "updateTime": {
        "bsonType": "number",
        "description": "updateTime is required, should be number"
      }
    }
  }
}